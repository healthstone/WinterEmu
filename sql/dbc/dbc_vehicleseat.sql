DROP TABLE IF EXISTS dbc.dbc_vehicleseat;

CREATE TABLE dbc.dbc_vehicleseat
(
    id                          INTEGER PRIMARY KEY DEFAULT 0,
    flags                       INTEGER NOT NULL    DEFAULT 0,
    attachment_id               INTEGER NOT NULL    DEFAULT 0,
    attachment_offset_x         REAL    NOT NULL    DEFAULT 0,
    attachment_offset_y         REAL    NOT NULL    DEFAULT 0,
    attachment_offset_z         REAL    NOT NULL    DEFAULT 0,
    enter_pre_delay             REAL    NOT NULL    DEFAULT 0,
    enter_speed                 REAL    NOT NULL    DEFAULT 0,
    enter_gravity               REAL    NOT NULL    DEFAULT 0,
    enter_min_duration          REAL    NOT NULL    DEFAULT 0,
    enter_max_duration          REAL    NOT NULL    DEFAULT 0,
    enter_min_arc_height        REAL    NOT NULL    DEFAULT 0,
    enter_max_arc_height        REAL    NOT NULL    DEFAULT 0,
    enter_anim_start            INTEGER NOT NULL    DEFAULT 0,
    enter_anim_loop             INTEGER NOT NULL    DEFAULT 0,
    ride_anim_start             INTEGER NOT NULL    DEFAULT 0,
    ride_anim_loop              INTEGER NOT NULL    DEFAULT 0,
    ride_upper_anim_start       INTEGER NOT NULL    DEFAULT 0,
    ride_upper_anim_loop        INTEGER NOT NULL    DEFAULT 0,
    exit_pre_delay              REAL    NOT NULL    DEFAULT 0,
    exit_speed                  REAL    NOT NULL    DEFAULT 0,
    exit_gravity                REAL    NOT NULL    DEFAULT 0,
    exit_min_duration           REAL    NOT NULL    DEFAULT 0,
    exit_max_duration           REAL    NOT NULL    DEFAULT 0,
    exit_min_arc_height         REAL    NOT NULL    DEFAULT 0,
    exit_max_arc_height         REAL    NOT NULL    DEFAULT 0,
    exit_anim_start             INTEGER NOT NULL    DEFAULT 0,
    exit_anim_loop              INTEGER NOT NULL    DEFAULT 0,
    exit_anim_end               INTEGER NOT NULL    DEFAULT 0,
    passenger_yaw               REAL    NOT NULL    DEFAULT 0,
    passenger_pitch             REAL    NOT NULL    DEFAULT 0,
    passenger_roll              REAL    NOT NULL    DEFAULT 0,
    passenger_attachment_id     INTEGER NOT NULL    DEFAULT 0,
    vehicle_enter_anim          INTEGER NOT NULL    DEFAULT 0,
    vehicle_exit_anim           INTEGER NOT NULL    DEFAULT 0,
    vehicle_ride_anim_loop      INTEGER NOT NULL    DEFAULT 0,
    vehicle_enter_anim_bone     INTEGER NOT NULL    DEFAULT 0,
    vehicle_exit_anim_bone      INTEGER NOT NULL    DEFAULT 0,
    vehicle_ride_anim_loop_bone INTEGER NOT NULL    DEFAULT 0,
    vehicle_enter_anim_delay    REAL    NOT NULL    DEFAULT 0,
    vehicle_exit_anim_delay     REAL    NOT NULL    DEFAULT 0,
    vehicle_ability_display     INTEGER NOT NULL    DEFAULT 0,
    enter_ui_sound_id           INTEGER NOT NULL    DEFAULT 0,
    exit_ui_sound_id            INTEGER NOT NULL    DEFAULT 0,
    ui_skin                     INTEGER NOT NULL    DEFAULT 0,
    flags_b                     INTEGER NOT NULL    DEFAULT 0,
    camera_entering_delay       REAL    NOT NULL    DEFAULT 0,
    camera_entering_duration    REAL    NOT NULL    DEFAULT 0,
    camera_exiting_delay        REAL    NOT NULL    DEFAULT 0,
    camera_exiting_duration     REAL    NOT NULL    DEFAULT 0,
    camera_offset_x             REAL    NOT NULL    DEFAULT 0,
    camera_offset_y             REAL    NOT NULL    DEFAULT 0,
    camera_offset_z             REAL    NOT NULL    DEFAULT 0,
    camera_pos_chase_rate       REAL    NOT NULL    DEFAULT 0,
    camera_facing_chase_rate    REAL    NOT NULL    DEFAULT 0,
    camera_entering_zoom        REAL    NOT NULL    DEFAULT 0,
    camera_seat_zoom_min        REAL    NOT NULL    DEFAULT 0,
    camera_seat_zoom_max        REAL    NOT NULL    DEFAULT 0
);
